The GameObject Material need to be set mesh collider, and not select convex.

The Texture need select Advanced -> Read/Write Enabled

GetPixelColor : "color" in Inspector is for show pixel color when mouse button down
SetPixelColor : "color" in Inspector is for change color when mouse button down